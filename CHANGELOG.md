# 📝 更新日志 (Changelog)

本文档记录了 `astrbot_plugin_endworld_img_api` 插件的所有重要版本更新与变更。

---

## [v6.0.0] 
### 🐛 修复 (Fixes)
* **核心通信修复**：回滚并修复了过度的平台解耦优化，完美恢复了原生底层协议（如 OneBot/NapCat）的合并转发防风控逻辑，解决直发导致的秒拦截问题。
* **网络解析修复**：移除了会导致拥塞报错的阻塞式本地 DNS SSRF 校验，解决合规图源被误判为“请求危险”导致假拦截、无法出图的致命 Bug。

### ✨ 新增与优化 (Features & Optimizations)
* **目录规范化**：全面接入 AstrBot 的 `StarTools` 规范，缓存图片现统一存放在专用持久化数据目录，彻底告别全局工作空间污染。
* **异步安全增强**：新增 `_create_safe_task` 方法统一接管后台任务与异常回调，从根本上避免了大并发且遭遇文件系统锁时引发的隐蔽协程泄露与资源吞噬。
* **底层依赖升级**：全面兼容较新版本的 `aiohttp`，采用官方推荐的 `ssl.create_default_context()` 替换已被废弃的 `verify_ssl` 传参方式。

---

## [v5.9.0]
### ✨ 新增 (Features)
* **批量获取支持**：新增 `指令 + 空格 + 数量` 批量出图功能。
* **动态合并转发**：新增批量发图时的阈值判定，超量自动转为合并聊天记录，防刷屏又防封。

### 🚀 优化 (Optimizations)
* **重新抽卡机制**：图片发送失败（被风控）时，从“死磕重发废图”改为“重新调用 API 获取新图”，大幅提升体验。
* **破除 HTTP 缓存**：加入动态时间戳和 UA 伪装，解决部分套了 CDN 的 API 在群聊中总是返回同一张图的顽疾。
* **撤回逻辑优化**：提示文字强制独立发送，杜绝“合并卡片无法撤回”的问题。

---

## [v5.8.0]
### ✨ 新增与优化 (Features & Optimizations)
* **多图源自动轮询机制**：当前 API 失效时自动静默尝试下一个。
* **终极兜底逻辑优化**：仅在所有 API 全军覆没时才会触发直链兜底。

---

## [v5.7.1]
### 🛡️ 安全与防风控 (Security)
* **兜底直链强制合并转发**：彻底解决群内直发外部链接导致封号的风险。
* **独立图源合并发送选项**：允许正常图片也伪装在卡片内发出。

---

## [v5.6]
### ✨ 新增 (Features)
* **猫娘模式**：新增【猫娘模式】，支持自定义文字提示的后缀词。

---

## [v5.3 - v5.5]
### 🐛 修复 (Fixes)
* **撤回功能修复**：修复顶层框架 `message_id` 获取异常，改为直达底层协议实现 100% 撤回。

---

## [v5.0 - v5.2]
### ✨ 新增与安全 (Features & Security)
* 新增防 SSRF 机制与 OOM 保护。
* 新增分群黑白名单管理功能。
* 首次引入阅后即焚（自动撤回）功能。

---

## [v4.0 - v4.9]
### 🚀 架构升级 (Architecture)
* WebUI 架构重构。
* 引入本地缓存发送机制。
* 引入 `Pillow` 压缩大图防超时机制。
