# 📝 更新日志 (Changelog)

本文档记录了 `astrbot_plugin_endworld_img_api` 插件的所有重要版本更新与变更。

---
## [v6.5.1] - WebUI 编辑体验与兼容性修复

### 🐛 修复
* 修复新增图源删除按钮在受限 iframe 中无响应的问题：新图源直接删除，已保存图源改用页面内确认对话框。
* 修复添加或删除 API、触发词、群号及图源排序后卡片意外折叠的问题；多个展开状态会随图源稳定保留。
* 修复旧版配置包含额外字段或缺少新增字段时保存全部提示校验失败的问题。

### ✨ 改进
* 新增图源自动展开并聚焦名称，删除后自动展开相邻图源；保存或导入成功后同步清理新建状态。
* 校验失败时显示错误总数，展开并聚焦首个错误字段。
* WebUI 更新为兼顾亮暗模式和窄屏对比度的蓝粉渐变主题。

## [v6.5.0] - WebUI 配置中心

### ✨ 新增
* 新增 AstrBot 原生 Plugin Page，可在页面中管理全部图源与全局配置，并将修改同步到当前运行配置。
* 每个 API 地址旁新增独立检测按钮，直接检测尚未保存的输入值并展示 HTTP 状态、内容类型和耗时。
* 新增运行状态概览、JSON 配置导入预览与确认、完整配置导出、未保存更改提示，以及亮暗主题和窄屏布局。

### 🛡️ 安全与可靠性
* 后端统一进行配置类型、范围、枚举和 URL 校验；持久化失败时原地恢复配置对象。
* API 检测限制协议、私有地址、重定向次数、响应大小和超时，并逐次重新校验重定向目标。
* 导入文件限制为 256 KiB，使用随机临时文件并在处理后清理。

---
## [v6.4.0] - 企业级稳定与高并发重构版 (Enterprise Overhaul)
### ⚡ 性能革命 (Performance)
* **并发网络下载**：重构核心下载逻辑，引入 `asyncio.gather` 将批量获取（如获取 10 张图）从阻塞式串行升级为全并发执行。极大地压榨了网络 I/O 性能，提升出图速度。
* **全局连接池复用**：引入单例模式维护 `aiohttp.ClientSession` 与 `SSLContext`。消除重复建立 TCP 握手的巨大开销，彻底激活 Keep-Alive 连接池生命力。

### 🛡️ 内存安全与防御 (Security & Safety)
* **垃圾回收 (GC) 免疫机制**：引入了后台任务（文件延迟清理、消息延迟撤回）强引用集合 (`self._background_tasks`)，彻底修复了 Python 异步设计中弱引用协程被垃圾回收器意外抹杀的“幽灵 Bug”。
* **抗慢速 DNS 阻塞**：为 SSRF 校验底层的 `socket.getaddrinfo` 增加了严格的 5.0 秒超时保护，免疫恶意图源发起的 DNS 慢速放大攻击。

### ✨ 现代化增强 (Features)
* **精准扩展名探测**：废弃了粗略的头部字节切片匹配，全面拥抱原生 `mimetypes` 模块，通过 HTTP 响应头精准判定文件后缀。
* **优雅生命周期释放**：新增 `terminate()` 钩子，插件卸载时安全销毁会话资源，防止系统句柄泄露。

---

## [v6.2.0 - v6.3.0] - 跨平台协议兼容版
### 🐛 兼容性与修复 (Fixes)
* **原生撤回与多端防风控修复**：在保持跨平台抽象的基础上，重新适配了 OneBot/NapCat 等协议的高优先级 API（合并转发节点构造与字典型 MessageID 解析）。完美解决纯净模式下多图被 QQ 秒吞、提示词无法撤回的问题。
* **Fake-IP 代理兼容**：SSRF 模块添加了智能网段嗅探，完美支持 Clash/Surge/V2ray 产生的 `198.18.0.0/15` Fake-IP 虚拟网络环境，公网 API 不再遭遇“被解析为内网危险 IP”的致命拦截。
* **异常全覆盖追踪**：扫除所有会导致线上死角的无声报错 (`except: pass`)，对接标准化 `logger` 追踪。
* **类型推断全覆盖**：重构并运用了 Python 3.10+ 原生类型提示 (Type Hints)。

---
## [v6.1.1] - 网络环境兼容性热修复
### 🐛 修复 (Fixes)
* **DNS 阻塞误杀修复**：移除了过于激进的底层 `socket.getaddrinfo` 强制 DNS 解析。修复了在 Docker 容器或旁路由代理环境下，合法公网 API（如 anosu.top 等）因无法直接被 Python 解析而被误判拦截的致命 Bug。

---

## [v6.1.0] - 性能与架构重构版 (Major Overhaul)
### ⚡ 性能优化 (Performance)
* **拯救事件循环（核弹级优化）**：将极其消耗 CPU 资源的 `Pillow` 图像压缩操作（`_compress_image`）全面迁移至 `asyncio.to_thread` 独立线程池中执行。彻底解决了大图处理时导致 AstrBot 主线程假死、无法响应其他群友指令的性能瓶颈。
* **惰性冷却清理**：优化了 `_check_cooldown` 机制，改用阈值触发（长度>50）的惰性字典清理策略，大幅降低了高并发海量请求下的 CPU 计算开销。

### 🛡️ 安全增强 (Security)
* **深层 SSRF 终极防御**：禁用了 `aiohttp` 的默认跟随重定向（`allow_redirects=False`），手动接管并递归检验 `30x` 状态码中的 `Location`，彻底封堵了攻击者通过公网 API 诱导重定向至内网敏感端口的漏洞。
* **高级 IP 绕过拦截**：结合 `ipaddress` 模块，现可完美拦截利用十进制（如 `2130706433`）或十六进制（如 `0x7f000001`）等变体格式发起的内网绕过攻击。

### 🛠️ 重构与优化 (Refactoring)
* **轮询池调度算法**：重构了多图源请求逻辑，采用 O(n) 的 Round-Robin 算法（`_download_image_with_retry`）替代了原本低效的嵌套遍历。极大减少了因全图源失效带来的无效网络阻塞。
* **圈复杂度降级**：将过于臃肿的 `_process_and_send` 拆分为清晰的 `_handle_batch_forward` 与 `_handle_single_send` 独立控制流，提升代码可读性与健壮性。
* **全覆盖日志追踪**：移除了所有不规范的静默异常捕获（`except: pass`），全面接入 `logger` 追踪系统，让排查故障不再是盲人摸象。

---
## [v6.0.0] 
### 🐛 修复 (Fixes)
* **核心通信修复**：回滚并修复了过度的平台解耦优化，完美恢复了原生底层协议（如 OneBot/NapCat）的合并转发防风控逻辑，解决直发导致的秒拦截问题。
* **网络解析修复**：移除了会导致拥塞报错的阻塞式本地 DNS SSRF 校验，解决合规图源被误判为“请求危险”导致假拦截、无法出图的致命 Bug。

### ✨ 新增与优化 (Features & Optimizations)
* **目录规范化**：全面接入 AstrBot 的 `StarTools` 规范，缓存图片现统一存放在专用持久化数据目录，彻底告别全局工作空间污染。
* **异步安全增强**：新增 `_create_safe_task` 方法统一接管后台任务与异常回调，从根本上避免了大并发且遭遇文件系统锁时引发的隐蔽协程泄露与资源吞噬。
* **底层依赖升级**：全面兼容较新版本的 `aiohttp`，采用官方推荐的 `ssl.create_default_context()` 替换已被废弃的 `verify_ssl` 传参方式。

---

## [v5.9.0]
### ✨ 新增 (Features)
* **批量获取支持**：新增 `指令 + 空格 + 数量` 批量出图功能。
* **动态合并转发**：新增批量发图时的阈值判定，超量自动转为合并聊天记录，防刷屏又防封。

### 🚀 优化 (Optimizations)
* **重新抽卡机制**：图片发送失败（被风控）时，从“死磕重发废图”改为“重新调用 API 获取新图”，大幅提升体验。
* **破除 HTTP 缓存**：加入动态时间戳和 UA 伪装，解决部分套了 CDN 的 API 在群聊中总是返回同一张图的顽疾。
* **撤回逻辑优化**：提示文字强制独立发送，杜绝“合并卡片无法撤回”的问题。

---

## [v5.8.0]
### ✨ 新增与优化 (Features & Optimizations)
* **多图源自动轮询机制**：当前 API 失效时自动静默尝试下一个。
* **终极兜底逻辑优化**：仅在所有 API 全军覆没时才会触发直链兜底。

---

## [v5.7.1]
### 🛡️ 安全与防风控 (Security)
* **兜底直链强制合并转发**：彻底解决群内直发外部链接导致封号的风险。
* **独立图源合并发送选项**：允许正常图片也伪装在卡片内发出。

---

## [v5.6]
### ✨ 新增 (Features)
* **猫娘模式**：新增【猫娘模式】，支持自定义文字提示的后缀词。

---

## [v5.3 - v5.5]
### 🐛 修复 (Fixes)
* **撤回功能修复**：修复顶层框架 `message_id` 获取异常，改为直达底层协议实现 100% 撤回。

---

## [v5.0 - v5.2]
### ✨ 新增与安全 (Features & Security)
* 新增防 SSRF 机制与 OOM 保护。
* 新增分群黑白名单管理功能。
* 首次引入阅后即焚（自动撤回）功能。

---

## [v4.0 - v4.9]
### 🚀 架构升级 (Architecture)
* WebUI 架构重构。
* 引入本地缓存发送机制。
* 引入 `Pillow` 压缩大图防超时机制。
